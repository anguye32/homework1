What is the difference between a merge and a rebase?

Merge combines multiple lines of development into a single branch, while rebase applies changes from one branch onto another.

What is a remote repository?

A project that is hosted on the internet or network that facilitates collaboration by other users which allows features such as pushing or pulling changes.